{
  "name": "openquality-cloud",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "eleventy --serve --watch",
    "build": "eleventy",
    "preview": "eleventy --serve --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "luxon": "^3.7.2",
    "markdown-it": "^14.1.0"
  }
}
